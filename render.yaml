services:
  - type: web
    name: ai-stock-predictor
    env: python
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: python -m gunicorn dashboard.app:server
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0